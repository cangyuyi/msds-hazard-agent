# 产品简述

职业卫生人员需要从 MSDS 的“成分/组成信息”判断物质是否处于职业病危害因素知识表及高毒目录中。原流程依赖人工翻页、复制 CAS、查表；扫描件和供应商版式差异会造成遗漏与误判。

本项目把这项工作拆为可审计的步骤：先抽取页码、名称、原始 CAS、含量；再以 CAS 优先匹配知识表；最后按置信度进入自动通过、人工确认或必须复核。系统明确把“知识表未命中”与“没有危害”分开。

V2 只新增本地 OCR，不改变评测集与判定规则：解决了字体编码异常 PDF 的 CAS 复核，也记录下 `Sn → Sm` 的 OCR 误识别案例，作为后续迭代依据。
